{
  "name": "ysc",
  "displayName": "ysc support for VS Code",
  "description": "Powerful editor support for ysc language using the built-in LSP.",
  "version": "0.1.0",
  "publisher": "Yanis",
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Programming Languages",
    "Linters",
    "Formatters"
  ],
  "main": "./src/extension.js",
  "activationEvents": [],
  "contributes": {
    "languages": [
      {
        "id": "ysc",
        "aliases": ["ysc"],
        "extensions": [".ys"],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "ysc",
        "scopeName": "source.ysc",
        "path": "./syntaxes/ysc.tmLanguage.json"
      }
    ],
    "configuration": {
      "title": "ysc",
      "properties": {
        "ysc.serverPath": {
          "type": "string",
          "default": "ysc-lsp",
          "description": "Absolute path to the ysc-lsp binary or command name if in PATH."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "echo 'No compilation needed for basic JS client'"
  },
  "dependencies": {
    "vscode-languageclient": "^8.0.2"
  }
}
